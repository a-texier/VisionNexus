#################################
# requirements.txt - VisionNexus Tracker
# Cible : Linux x86_64 + GPU NVIDIA recent (CUDA 12.x)
#         CUDA 12.1 / cuDNN 8.9.7 / TensorRT 8.6.1 LTS
#         Python 3.10.12
#
# Installer avec :
#   pip install -r requirements.txt
#
# ATTENTION : torch doit etre installe en PREMIER avec l'URL CUDA correcte.
#   Voir setup_rtxa1000.sh pour la sequence complete.
#   pip install torch==2.5.1+cu121 torchvision==0.20.1+cu121 \
#       --extra-index-url https://download.pytorch.org/whl/cu121
#################################


#################################
# 1. DEEP LEARNING - GPU
#################################

# PyTorch 2.5.1 cu121 - contrainte dure de SAM2 + support CC 8.6
# NE PAS installer via pip install torch seul (prend la version CPU).
torch==2.5.1
torchvision==0.20.1

# ONNX Runtime avec support GPU (inference ONNX - optionnel si TRT utilise)
onnxruntime-gpu==1.19.2

# ONNX export (YOLODetectorMOT : .onnx, .engine)
onnx==1.16.2


#################################
# 2. VISION + DETECTION
#################################

# OpenCV avec modules contrib (CSRT, ORB, ECC)
# OBLIGATOIRE : opencv-contrib-python (pas opencv-python - CSRT absent)
# Utilise dans : pipeline/command_parser.py, data/convert_format specialise_to_png.py,
#                data/generate_synthetic.py, utils/stream_server.py,
#                trackers/sot/csrt/tracker.py, trackers/sot/tracking_tophat/tracker.py
opencv-contrib-python==4.10.0.84

# Ultralytics YOLO (detecteur MOT - import lazy dans detector_mot.py)
# Supporte .pt, .onnx, .engine (TensorRT)
ultralytics==8.3.40


#################################
# 3. CALCUL SCIENTIFIQUE
#################################

# NumPy 1.26.x - contrainte forte : pytracking (DiMP) compile des extensions
# Cython contre NumPy 1.x ABI. NumPy 2.x casse ces extensions.
numpy==1.26.4

# SciPy - utilise dans trackers/mot/custom_kalman/mot_tracker.py
# (scipy.optimize.linear_sum_assignment - algorithme Hongrois)
scipy==1.13.1

# Pandas - utilise par les repos clones (ByteTrack, BoT-SORT, OSTrack, DiMP)
# Non importe directement dans notre code pipeline/utils/data
pandas==2.2.3

# Matplotlib - utils/metrics_plot.py (PNG metriques - import lazy avec fallback)
matplotlib==3.9.4

# Plotly - utils/metrics_plot.py (HTML interactif centroide + stabilite ID)
# Import lazy : si absent, les graphes HTML sont skipped avec un warning.
plotly==5.24.1

# Scikit-learn - utils/metrics_plot.py (courbe ROC - import lazy avec fallback)
scikit-learn==1.5.2


#################################
# 4. RESEAU / IPC
#################################

# pyzmq - data/zmq_reader.py, data/zmq_display_bridge.py
# Flux frames temps reel depuis camera MultiCsv via ZeroMQ
pyzmq==26.2.0


#################################
# 5. TRACKING - MOT TIERS
#################################

# LAP - Linear Assignment Problem (ByteTrack + BoT-SORT internes)
# lapx = fork maintenu de lap, meme API (`import lap` / lapjv) mais avec WHEEL
# pre-built cp310 -> aucune compilation (lap==0.4.0 ne compile plus avec les
# toolchains recents). C'est aussi la dependance utilisee par Ultralytics.
lapx==0.5.11

# cython_bbox - IoU vectorise en C pour BoT-SORT / ByteTrack (matching.py).
# DESACTIVE : la version 0.1.3 (seule dispo) crashe a l'import avec numpy>=1.24
# (utilise `np.float`, supprime), or on pin numpy==1.26.4. Les matching.py ont un
# fallback numpy pur declenche sur ImportError -> ne pas l'installer = repli propre
# (accel C en moins, resultats identiques). Reactiver seulement si un cython_bbox
# compatible numpy>=1.24 devient disponible.
# cython_bbox==0.1.3

# Cython (compilation des extensions pytracking DiMP)
Cython==3.0.11

# filterpy (Kalman filter - utilise par certaines versions ByteTrack/BoT-SORT)
filterpy==1.4.5


#################################
# 6. LOGGING + UTILITAIRES
#################################

# PyYAML - main.py (chargement config.yaml)
PyYAML==6.0.2

# tqdm (barres de progression - optionnel, utilise par pytracking)
tqdm==4.67.1

# easydict (format dict de type objet - pytracking DiMP)
easydict==1.13

# einops (transformations tenseurs - OSTrack, SAM2)
einops==0.8.0

# timm - PyTorch Image Models (OSTrack backbone)
timm==1.0.12

# huggingface hub (download poids SAM2 si non fournis)
huggingface-hub==0.26.5

# Pillow - utilise par matplotlib (rendu PNG) et reference dans utils/logger.py
Pillow==11.0.0

# psutil - optionnel, monitoring RAM/VRAM en debug (non importe dans pipeline)
psutil==6.1.0


#################################
# 7. DONNEES format specialise (MultiCsv)
#################################

# optional_format_adapter - lecteur fichiers .optional MultiCsvCamera
# Paquet interne (fourni separement, hors PyPI) - installer depuis le chemin fourni :
#   pip install /opt/vendor/optional_format_adapter-<version>.whl
# Si absent, le loader bascule automatiquement sur PNG/JPG.
# optional_format_adapter>=1.0.0   # decommenter et adapter la version


#################################
# 8. QUALITE / DEV
#################################

# Outils lances par quality.py - ne pas installer sur la cible embarquee Jetson
ruff>=0.8.0
mypy>=1.13.0
bandit>=1.8.0
pytest>=8.3.0
pytest-cov>=6.0.0


#################################
# 9. DEPENDANCES INDIRECTES (versions figees pour reproductibilite)
#################################

requests==2.32.3
packaging==24.2
filelock==3.16.1
typing_extensions==4.12.2
sympy==1.13.1  # pin impose par torch==2.5.1 (torch depend de sympy==1.13.1)
networkx==3.4.2
jinja2==3.1.4
mpmath==1.3.0
